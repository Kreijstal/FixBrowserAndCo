use "classes";

import "util/common";

function adjust_document(request: Request, document: Element)
{
	foreach (var img in document.query_all("img[data-img]")) {
		img.set_attr("src", img.get_attr("data-img"));
	}
}
