use "classes";

import "util/common";

function is_same_domain(src: String, dest: String): Boolean
{
	if (domain_ends_with(dest, "cloudinary.com")) return true;
	if (domain_ends_with(dest, "fastly.net")) return true;
	return false;
}
