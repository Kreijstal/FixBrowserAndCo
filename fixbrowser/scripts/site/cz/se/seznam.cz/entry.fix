use "classes";

import "util/common";

function is_same_domain(src: String, dest: String): Boolean
{
	if (domain_ends_with(dest, "szn.cz")) return true;
	return false;
}

function adjust_document(request: Request, document: Element)
{
	/*
	TODO: doesn't work
	if (request.get_domain() == "videa.seznam.cz") {
		foreach (var elem in document.query_all("img.Video-header-thumbnail")) {
			var src = elem.get_attr("src");
			request.set_same_domain_url(src);
		}
	}
	*/
}
