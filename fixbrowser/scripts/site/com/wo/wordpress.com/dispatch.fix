use "classes";

import "util/common";

function adjust_document(request: Request, document: Element)
{
	// TODO: CSS returned empty from server *.wp.com
	request.add_same_domain("wp.com");
	request.add_same_domain("wordpress.com");
}
